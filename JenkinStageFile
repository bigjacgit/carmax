#!/usr/bin/env groovy

//@Library('my-shared-library') _

@Library('hyperloop-db@db-template') _

def stages = [
    'Github Checkout',
    'Flyway Info & Validate',
    'Create RDS Snapshot',
    'Migrate SQLs',
    'Validate changes'
]

def nodeName = "aws-agents-all-regions"

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}

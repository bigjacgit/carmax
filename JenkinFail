pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'make check'
            }
        }
    }
    post {
        always {
           sh 'echo thanks'
           }
        failure {
           sh 'echo failed'
        }
    }
}
